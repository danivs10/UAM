Para ejecutar el programa:
    -entrar en la carpeta /app y ejecutar el comando "python3 app.py"
Pra ejecutar los .sql:
    -entrar en la carpeta /app y ejecutar el comando "cat <<Nombre del archivo>>.sql | psql -U alumnodb -d si1"

